package Access_Modifiers;

public class AccessModifiersz {

	
	
	
	/*
	In Java, access modifiers control the visibility and accessibility of classes, methods, and variables. The four main access modifiers are:

		Public (public)

		Accessible from anywhere in the program (within the same class, package, or different packages).
		Used for methods and variables that should be widely available.
		-------------------------------------------------------------------------------------------------------
		
		Private (private)

		Accessible only within the same class.
		Not visible to subclasses or other classes in the same package.
		Typically used to encapsulate class details and enforce data hiding.
		-------------------------------------------------------------------------------------------------------
		
		Protected (protected)

		Accessible within the same package and by subclasses (even in different packages).
		Used when a class should allow inheritance while restricting direct access.
		-------------------------------------------------------------------------------------------------------
		
		Default (Package-Private) (No Modifier)

		Accessible only within the same package (not from outside the package).
		Used when access should be limited to related classes within the same package.
		These modifiers help enforce encapsulation and security in Java programs.
		-------------------------------------------------------------------------------------------------------
		// Maybe included already====> How about writing methods that return data and How would one use it ?
		 * 
		 * */
		 
}
